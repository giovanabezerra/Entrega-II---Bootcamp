{
  "name": "bootcamp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "node scripts/build-extension.mjs",
    "test:e2e": "playwright test",
    "test": "npm run build && npm run test:e2e",

    /* ATENÇÃO: ESSA LINHA É A CORREÇÃO CRÍTICA */
    "test:ci": "npm run build && npx playwright test --reporter=junit,html",

    "test:docker": "docker compose run --rm playwright"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@playwright/test": "^1.45.3"
  }
}
